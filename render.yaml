services:
  - type: worker
    name: gastobot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: SHEETS_WEBHOOK
        sync: false
      - key: SEU_TELEGRAM_ID
        sync: false
